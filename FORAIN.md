# Forain MVP

Forain은 사용자가 남긴 편린에서 실제로 수행한 활동을 추출하고, 확인된 활동을 개인 결숲의 생장으로 시각화하는 PC 우선 웹앱입니다.

## 구현된 흐름

1. 아이디·비밀번호로 회원가입 또는 로그인
2. 편린 작성 및 저장
3. 활동 분석
4. 추출 결과의 이름과 카테고리 수정 또는 삭제
5. 결과 확정
6. 결숲에서 활동 줄기 확인
7. 줄기 선택 후 근거 문장과 생장도 확인

같은 활동 분석 결과를 다시 확정해도 activity_mention_id 고유 제약으로 생장 이벤트가 중복 생성되지 않습니다. 편린이나 활동을 수정 또는 삭제하면 해당 날짜의 생장도를 다시 계산합니다.

일일/카테고리별 성장 상한은 현재 적용되어 있지 않습니다(테스트를 위해 제거된 상태이며, 복구 여부는 아직 결정되지 않았습니다).

## 환경변수

`.env.example`을 참고합니다. 자세한 값 설명은 [README.md](README.md#환경변수)를 참고하세요.

- `TURSO_DATABASE_URL` / `TURSO_AUTH_TOKEN`: Turso 데이터베이스 연결 정보
- `AI_MOCK_MODE`: `true`면 네트워크 없이 규칙 기반 분석기를 사용합니다
- `OPENAI_API_KEY`: 실제 OpenAI 분석을 사용할 때 서버 환경에만 설정합니다
- `OPENAI_ACTIVITY_MODEL`: 기본 분석 모델입니다

비밀키를 클라이언트 코드나 Git에 저장하지 마세요.

## 로컬 실행

```sh
npm install
npm run db:migrate
npm run dev
```

개발 서버가 표시하는 Local URL을 사용합니다. 로컬 미리보기도 배포판과 동일한 Turso DB/자체 계정 로그인 흐름을 그대로 사용합니다.

## 로그인 보안

같은 아이디로 비밀번호를 5회 연속 틀리면 15분간 로그인이 잠깁니다. 잠금 시간이 지나면 다음 시도 때 자동으로 해제되며, 존재하지 않는 아이디로 시도한 경우에도 동일한 오류 메시지만 노출해 계정 존재 여부를 알 수 없게 했습니다. (IP 기반 제한은 아직 없습니다 — 아래 "현재 미구현" 참고.)

## 데이터베이스 마이그레이션

스키마는 `db/schema.ts`에 있습니다. 스키마를 변경한 후 `npm run db:generate`로 새 마이그레이션을 생성하고 `npm run db:migrate`로 Turso DB에 적용합니다.

기존에 적용된 마이그레이션은 수정하지 않고 새 파일을 추가합니다. 마이그레이션 SQL 파일의 여러 문장 사이에는 `--> statement-breakpoint` 구분자가 있어야 합니다 — 없으면 Turso가 마이그레이션 적용 자체를 거부합니다 (직접 SQL을 편집했을 때 특히 주의).

## Mock AI

`AI_MOCK_MODE=true`(기본값)일 때는 규칙 기반 분석기를 사용합니다. 실행, 계획, 타인의 활동, 과거 회상, 실패한 시도를 구분하는 기본 규칙을 포함합니다.

    오늘 기타를 연습했다.
    내일은 운동해야겠다.
    친구가 축구를 했고 나는 옆에서 책을 읽었다.
    오늘은 아무것도 하지 않고 쉬었다.

기대 결과는 기타 연습, 독서, 휴식입니다.

## 실제 OpenAI API

`AI_MOCK_MODE=false`로 설정하고 `OPENAI_API_KEY`를 넣으면 실제 OpenAI Responses API로 분석합니다. 서버 라우트만 API 키를 읽으며, 일기 원문을 로그에 남기지 않습니다. 분석 결과는 JSON Schema로 제한합니다.

## 결숲 시각화

결숲은 카테고리별 누적 생장도를 기반으로 매 렌더링마다 클라이언트에서 새로 계산되는 SVG 구조입니다 — 가지 모양 자체는 DB에 저장되지 않고, 카테고리 이름으로 시드(seed)를 만든 결정적 난수로 항상 같은 점수엔 같은 모양이 재현됩니다.

- **유기적 성장**: 가지 각도·길이·휘어짐에 랜덤성을 줘서 대칭적인 프랙탈이 아니라 손으로 기른 듯한 모양을 만듭니다.
- **위쪽 성장**: 생장도가 높은 줄기는 원래 방향과 무관하게 위쪽으로 휘어지는 곁가지를 추가로 냅니다.
- **점진적 감속 성장**: 두께·잎 크기·길이 등은 일정 점수까지는 선형으로, 그 이후엔 제곱근 곡선으로 계속 자라서 아주 높은 점수에서도 시각적으로 정체되지 않습니다.
- **중앙 핵 성장**: 전체(모든 카테고리 합산) 생장도에 비례해 중앙 뿌리 뭉치도 커지고 발광이 진해집니다.
- **분위기 요소**: 전체 생장도가 쌓일수록 배경에 장식용 실루엣 나무가 늘어나고 옅은 안개가 짙어집니다 (실제 카테고리 데이터와는 무관한 장식).

## 주요 파일

- `app/forain-app.tsx`: 대시보드, 편린, 분석 결과, 결숲 시각화(SVG 프랙탈 생성, 중앙 핵, 배경 분위기), 설정 UI
- `app/auth.ts`: 비밀번호 해싱, 세션 발급/검증, 로그인 시도 제한(계정 잠금)
- `app/globals.css`: 결숲과 유리 질감(liquid glass)의 시각 시스템 및 반응형 레이아웃
- `app/api/`: 일기, 활동 분석, 확정, 인증 API
- `lib/forain-server.ts`: Mock/실제 AI 분석과 일일 상한 재계산
- `lib/database.ts`: Turso(libSQL)를 D1과 같은 인터페이스로 감싸는 어댑터
- `db/schema.ts`: Turso 데이터 모델
- `public/forain-logo.png`: 브랜드 로고

## 현재 미구현

- 공개 결숲과 사용자 간 공명 (다른 유저의 결숲을 볼 방법이 없음)
- 날씨, 계절, 시간대 효과
- 3D 생태계
- 소셜 로그인과 결제
- 음성 일기
- 감정 또는 성격 분석 (의도적으로 제외 — AI 프롬프트에서 감정/성격 추론을 금지하고 있음)
- 비밀번호 재설정/변경 (이메일을 수집하지 않아 복구 수단이 없음 — 로그인 중 눈 아이콘으로 입력값 확인만 가능)
- 계정 셀프서비스 삭제 (현재는 문의 후 수동 처리)
- IP 기반 로그인 시도 제한 (계정 단위 잠금만 있음)
- 일일/카테고리별 성장 상한 (테스트 목적으로 제거된 뒤 복구 여부 미결정)
